apiVersion: v1
kind: Service
metadata:
  name: {{ include "houndr.fullname" . }}
  labels:
    {{- include "houndr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "houndr.selectorLabels" . | nindent 4 }}
